# Minimal Niri Bar Configuration
# This file tests the default values - everything should work with just this!

# This is intentionally minimal to test defaults
# All other settings should use sensible defaults
